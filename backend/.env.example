# Plantilla de entorno del backend de SAVES.
#
# Copiar a `.env` y rellenar. NUNCA se versiona el `.env` real (lo cubre .gitignore);
# esta plantilla existe porque sin ella las ~18 variables sólo vivían en el servidor
# y reconstruir el entorno desde el repositorio era imposible.
#
# `ecosystem.config.js` lee este mismo fichero y ABORTA el arranque si falta alguna
# de las marcadas como obligatorias.
#
#   chmod 600 .env      ← contiene secretos en claro

# ── Obligatorias: sin ellas no arranca en producción ────────────────────────
DATABASE_URL="postgresql://usuario:clave@localhost:5432/saves_vestel?schema=public"
# Orígenes permitidos, separados por coma. Debe incluir el dominio público.
CORS_ORIGIN="https://app.saves.com.co,http://localhost:3060"
# Firma de los JWT. Generar con: openssl rand -hex 32
# DEBE coincidir con el AUTH_SECRET del frontend (.env.local): el middleware del
# edge verifica en el frontend el mismo token que firma el backend.
AUTH_SECRET=

# Cifrado en reposo de las credenciales de routers y OLTs (AES-256-GCM).
# Generar con: openssl rand -hex 32
# ⚠️ INDEPENDIENTE de AUTH_SECRET a propósito: `secret-box` usa AUTH_SECRET como
# respaldo si esta falta, y entonces rotar el secreto de sesión dejaría ILEGIBLES
# todas las contraseñas de los equipos. Si se rota esta, hay que re-cifrarlas
# (scripts/encrypt-network-secrets.ts).
SECRET_ENC_KEY=

PORT=3061

# ── Interruptores peligrosos ────────────────────────────────────────────────
# En 'false' las operaciones se SIMULAN (dry-run) y quedan auditadas sin tocar los
# equipos. En 'true' se ejecutan de verdad. El arranque deja escrito en el log
# cuáles están en vivo.
MIKROTIK_LIVE=false      # corte/reconexión/alta PPPoE contra los routers reales
OLT_LIVE=false           # autorizar/reiniciar/eliminar ONUs por SSH
GENIEACS_LIVE=false      # acciones TR-069
CRONS_ENABLED=false      # facturación recurrente y cartera programadas

# ── Reglas de operación (no tocan equipos; cambian cómo se trabaja) ─────────
# Una orden a la vez: con 'true' nadie abre una segunda orden abierta —propia o
# ajena— hasta cerrar la que tiene encima. APAGADA a pedido del usuario el
# 2026-09-09; la regla sigue completa en src/support/turno.ts esperando aquí.
# OJO: al TÉCNICO DE CAMPO le aplica SIEMPRE desde el 2026-09-10, esté esto como
# esté — no puede saltarse órdenes de su agenda (ver `turnoAplicaA`).
UNA_ORDEN_A_LA_VEZ=false

# Geo-cerca del cierre de órdenes de campo: off | observar | exigir. Sin poner, manda
# el ajuste `tickets.geofence.mode` de la base, que está en 'exigir' desde el
# 2026-09-10: la visita a domicilio se cierra desde la casa del cliente y compartiendo
# la ubicación. Sólo aplica a los tipos de campo (~13% de los cierres).
# TICKET_GEOFENCE_MODE=exigir

# Registro fotográfico obligatorio para cerrar una visita a domicilio (2026-09-10).
# Encendido salvo que se ponga 'false' — la válvula para el día que una cuadrilla se
# quede sin cobertura para subir imágenes. Mismos exentos que la cerca, y los procesos
# automáticos (reconexión al pagar, sync) cierran sin foto porque cierran sin usuario.
TICKET_REQUIRE_PHOTO=true

# IP remota obligatoria para cerrar una visita a domicilio (2026-09-10): el cliente no
# se queda sin dirección fija, que es por donde sistemas entra a su equipo y la que el
# corte bloquea. Encendido salvo 'false'. No se le pide al abonado de sólo televisión
# (no tiene secret PPPoE) ni al retiro voluntario, y el bloqueo se resuelve desde la
# propia orden con "Asignar IP remota".
TICKET_REQUIRE_REMOTE_IP=true

# ── Integraciones (opcionales; sólo se exigen si su gate está encendido) ────
WA_AGENT_ENABLED=false
KAPSO_API_KEY=
KAPSO_PHONE_NUMBER_ID=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
# Sin este secreto el webhook RECHAZA todo (fail-closed, a propósito): es el único
# endpoint sin guard y la firma HMAC es su única barrera.
WHATSAPP_WEBHOOK_APP_SECRET=
WHATSAPP_ALERT_TO=

OPENAI_API_KEY=
WHATSAPP_BOT_MODEL=gpt-4o-mini
WHATSAPP_STT_MODEL=gpt-4o-mini-transcribe

# ── Recibo de caja (rollo térmico) ──────────────────────────────────────────
# Sólo hay que tocarlo si una sede pone una impresora distinta. El valor es el ancho
# que la impresora IMPRIME, no el que mide el rollo: una térmica de 80 mm marca unos
# 72, y una de 58 mm unos 48. Si el recibo sale cortado por la derecha, este número
# está por encima de lo que la impresora puede marcar.
RECIBO_ANCHO_MM=72
RECIBO_MARGEN_MM=2

# ── PlayHub (OTT/IPTV) ──────────────────────────────────────────────────────
# Las credenciales reales viven en el legacy (`variables_de_entorno`, fila PLAYHUB).
# Sin ApiKey/ApiSecret el módulo queda "no configurado" y no llama a la API.
PLAYHUB_BASE_URL=https://www.playhub.com.br/API/PlayhubApi
PLAYHUB_API_KEY=
PLAYHUB_API_SECRET=
# Regla de negocio: PlayHub sólo se asigna con internet de 100 Megas en adelante
# (0 = desactiva el guard).
PLAYHUB_MIN_MEGAS=100

# ── WEB SERVICE DEL PORTAL DE PAGOS EN LÍNEA (vestel.com.co/crm) ──────────────────
# El portal de pagos le pregunta a ESTE sistema (deuda, facturas, descuentos y pago)
# en vez de al legacy. Cinco métodos en /api/portal-pagos/*, con la misma forma que
# servía `Servicio.php` del legacy. Ver docs/portal-pagos.md.
PORTAL_WS_ENABLED=false
# md5 de las dos frases del apretón de manos que manda el portal en el cuerpo.
# Las frases viven en el PHP del portal, nunca aquí.
PORTAL_WS_USER_MD5=
PORTAL_WS_PASS_MD5=
# El MISMO par de frases, en el sentido contrario: con esto se le fija al abonado la
# contraseña del portal (crm/Servicio::update_user). Cuatro valores, una sola rotación.
PORTAL_CRM_URL=https://vestel.com.co/crm/servicio/
PORTAL_CRM_TOKEN_USUARIO=
PORTAL_CRM_TOKEN_CLAVE=
# Lista blanca de IPs desde las que se acepta la llamada (vacío = sin lista).
PORTAL_WS_IPS=127.0.0.1
# Cuenta donde entra la plata de la pasarela (legacy `accounts.id`).
PORTAL_WS_CASH_ACCOUNT_ID=23
PORTAL_CRM_BASE=https://vestel.com.co/crm
PORTAL_WS_RESCATE_DIAS=3
